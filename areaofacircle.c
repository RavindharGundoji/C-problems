#include<stdio.h>
#define PI  3.414

int main(){
   int r,i,d;
   scanf("%d",&r);
   float area;
   
   for(i=1;i == 1;i++)
   {
    d=r*r;
    break;
   }
  
  area = PI*d;
  
  printf("area of a circle with radius %d = %.2f is :",r,area);
  return 0;
}
